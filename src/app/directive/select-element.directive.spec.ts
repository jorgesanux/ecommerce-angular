import { SelectElementDirective } from './select-element.directive';

describe('SelectElementDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectElementDirective();
    expect(directive).toBeTruthy();
  });
});
